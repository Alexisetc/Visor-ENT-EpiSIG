# Pipeline ENT — dependencias Python
# Fase 1 (perfilado) usa solo pandas+pyreadstat+scipy+matplotlib+jinja2+pyarrow.
# Las demás se agregan desde la Fase 2 en adelante.

pandas>=2.0
pyreadstat>=1.2
pyarrow>=14
scipy>=1.10
statsmodels>=0.14
matplotlib>=3.7
jinja2>=3.1
openpyxl>=3.1
odfpy>=1.4
